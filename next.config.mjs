/** @type {import('next').NextConfig} */
const nextConfig = {
  // Try to optimize font loading even further if possible
  // Using experimental optimizeCss could be good but we don't need it if fonts are set to swap
};

export default nextConfig;
